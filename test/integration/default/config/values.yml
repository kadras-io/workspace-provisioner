---
apiVersion: v1
kind: Secret
metadata:
  name: workspace-provisioner-values
  namespace: tests
stringData:
  values.yaml: |
    namespaces:
      - name: test-default     
    oci_registry:
      secret:
        name: supply-chain-registry-credentials
